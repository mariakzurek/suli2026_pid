# DEPRECATED — use features_tier{1,2,3}.txt with train_bdt.py --features-file instead.
# This file is no longer read by the pipeline.
#
# For dataset column selection (build time), use:
#   scripts/training/columns_maximal.txt  (passed via --columns-file to build_dataset.py)
#
# For training feature selection (per run), use one of:
#   scripts/training/features_tier1.txt  — minimal FTOF+beta
#   scripts/training/features_tier2.txt  — adds chi2pid + FTOF 1A
#   scripts/training/features_tier3.txt  — adds ECAL + HTCC
# Passed via --features-file to train_bdt.py.

ftof_energy_1B
ftof_time_1B
ftof_path_1B 
chi2pid
beta

